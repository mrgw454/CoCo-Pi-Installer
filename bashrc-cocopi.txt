
systemtype=$(dpkg --print-architecture)

# detect model of Raspberry Pi
if [ -f /proc/device-tree/model ]; then

	RPI=`cat /proc/device-tree/model | cut -c14-16`

	if [[ "$RPI" == "500" ]]; then
		TITLE="RPi500"
	fi

	if [[ "$RPI" == "5 M" ]]; then
		TITLE="RPi5"
	fi

	if [[ "$RPI" == "400" ]]; then
		TITLE="RPi400"
	fi

	if [[ "$RPI" == "4 M" ]]; then
		TITLE="RPi4"
	fi

	if [[ "$RPI" == "3 M" ]]; then
		TITLE="RPi3"
	fi

	[ "$(uname -m)" = "aarch64" ] && export GDK_BACKEND=x11

else

	# for non ARM based systems
	TITLE=$(lscpu | grep 'Model name' | cut -f 2 -d ":" | awk '{$1=$1}1')

fi

echo $TITLE > $HOME/rpi-model.txt

# fix for the 'dialog' command when using a Putty client
export NCURSES_NO_UTF8_ACS=1

# fix for screen permission issue
export SCREENDIR=$HOME/.screen

# path settings
PATH=$HOME/.local/bin:$PATH:$HOME/.mame:/opt/mame:$HOME/.xroar:$HOME/.trs80gp:$HOME/scripts:$HOME/scripts/launcher:$HOME/scripts2

# add path for CC6303 compiler
export PATH=/opt/cc68/bin:$PATH

# add support for pyenv
export PYENV_ROOT="$HOME/.pyenv"
command -v pyenv >/dev/null || export PATH="$PYENV_ROOT/bin:$PATH"
eval "$(pyenv init -)"

export ACLOCAL_PATH=/usr/share/aclocal

# run new launcher menu from anywhere in a shell
launcher() {
    python3 "$HOME/scripts/launcher/launcher.py" "$@"
}




# launch CoCo-Pi menu if on local console only
if [ "$TERM" = linux ]; then
  cd $HOME
  launcher
else
  echo
  neofetch
fi

# opam configuration
test -r /home/ron/.opam/opam-init/init.sh && . /home/ron/.opam/opam-init/init.sh > /dev/null 2> /dev/null || true

# START of WSL2 specific
kernel=$(uname -a)

if echo "$kernel" | grep -q "WSL"; then

        # for vscode
        export DONT_PROMPT_WSL_INSTALL=Y

        echo
        echo "Use RIGHT mouse button or [SHIFT][CTRL][V] to paste inside this terminal."
        echo
        echo Current terminal size: $COLUMNS columns, $LINES lines.
        echo

        # add distro name to PS1 prompt
        PS1="$(echo "[${WSL_DISTRO_NAME}]") ${PS1} "

fi

# fix needed to get XRoar working properly when Wayland is the default
[ "$(uname -m)" = "aarch64" ] && export GDK_BACKEND=x11

# Run Windows CMD inside the Linux terminal (no wineconsole)
alias winecmd='WINEDLLOVERRIDES="wineconsole.exe=d" wine cmd'

